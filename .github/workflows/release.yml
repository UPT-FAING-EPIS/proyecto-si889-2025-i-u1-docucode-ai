name: Release PHP Project

on:
  push:
    tags:
      - 'v*'  # Este flujo se ejecutará cuando se haga un push a un tag que comience con 'v' (ej. v1.0.0)

jobs:
  release:
    name: Create Release
    runs-on: ubuntu-latest  # O 'windows-latest', según tu entorno

    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Set up PHP
      uses: shivammathur/setup-php@v2
      with:
        php-version: '7.4'  # O la versión de PHP que estás utilizando

    - name: Install dependencies
      run: |
        composer install --no-interaction --prefer-dist

    - name: Create the release archive
      run: |
        mkdir release
        tar -czf release/my-php-project-${GITHUB_REF##*/}.tar.gz *  # Crea un archivo .tar.gz con el código fuente

    - name: Create GitHub Release
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      run: |
        VERSION_TAG=${GITHUB_REF##*/}
        echo "Creating GitHub Release for version ${VERSION_TAG}"
        gh release create $VERSION_TAG release/my-php-project-${VERSION_TAG}.tar.gz --title "Release ${VERSION_TAG}" --notes "Release notes for version ${VERSION_TAG}"
